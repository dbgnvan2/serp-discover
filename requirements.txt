# Pinned with compatible ranges (Spec: seo_geo_review_20260704.md C.5).
# Core pipeline
pandas>=2.2,<4
openpyxl>=3.1,<4
requests>=2.32,<3
beautifulsoup4>=4.12,<5
pyyaml>=6.0,<7
ruamel.yaml>=0.17,<0.19  # round-trip YAML so serp-me.py save_config keeps config.yml comments
jsonschema>=4.21,<5
google-search-results>=2.4,<3
python-dotenv>=1.0,<2
anthropic>=0.40,<1
pytest>=8,<10
# GSC service-account auth (gsc_client.py — imported lazily; only needed
# when GSC_CREDENTIALS_PATH auth is actually used; seo_geo_deferred G.4)
google-auth>=2.30,<3
# Optional analysis extras — code degrades gracefully when absent
# (see serp_audit.py ImportError guards), but installed by default.
textblob>=0.18,<1
wordcloud>=1.9,<2
matplotlib>=3.8,<4
